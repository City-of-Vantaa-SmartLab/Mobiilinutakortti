version: '3.3'

services:
  nutakortti:
    ports:
      - 3000:3000
    build:
      context: ./
      dockerfile: Dockerfile
    image: nutakortti/latest
    environment:
      AUTH_SIGNKEY:
      CERT_SELECTION:
      FRONTEND_BASE_URL:
      HTTP_LOG_LEVEL:
      IDP_ENTITY_ID:
      JSON_LOGS:
      JWT:
      NODE_ENV:
      RDS_DB_NAME:
      RDS_HOSTNAME:
      RDS_PASSWORD:
      RDS_PORT:
      RDS_USERNAME:
      REACT_APP_ADMIN_FRONTEND_URL:
      REACT_APP_ENDPOINT:
      SP_ASSERT_ENDPOINT:
      SP_ENTITY_ID:
      SP_PKEY:
      SSO_LOGIN_URL:
      SSO_LOGOUT_URL:
      SUPER_ADMIN_FEATURES:
      TELIA_ENDPOINT:
      TELIA_PASSWORD:
      TELIA_USER:
      TELIA_USERNAME:
      AWS_SES_KEY_ID:
      AWS_SES_KEY_VALUE:
      EMAIL_SOURCE:
      RETURN_PATH:
      AWS_SES_REGION:
